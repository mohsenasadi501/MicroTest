apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
spec:
  rules:
  - host: mohsenasadi.com
    http:
      paths:
      - pathType: Prefix
        path: "/swagger"
        backend:
          service:
            name: platformservice
            port: 
              number: 80